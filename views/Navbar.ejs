<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navbar with Right-Aligned Buttons</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f8ff; /* Light pastel blue background for the body */
            color: #2c3e50; /* Dark text for readability */
        }

        /* Navbar Styles */
        .navbar {
            display: flex;
            justify-content: space-between;
            padding: 15px;
            align-items: center;
            background-color: #ffffff; /* Clean white navbar */
            color: #2c3e50; /* Dark text for navbar */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Soft shadow for depth */
        }

        .navbar-left a, .navbar-right a {
            color: #3498db; /* Soft blue for links */
            text-decoration: none;
            margin-right: 15px; /* Adds space between the buttons */
            font-size: 16px;
            padding: 10px;
            border-radius: 5px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .navbar-left a:hover, 
        .navbar-right a:hover {
            background-color: #3498db; /* Blue background on hover */
            color: #ffffff; /* White text on hover */
        }

        .navbar-right {
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }

        .navbar-right span,
        .navbar-right img,
        .navbar-right a {
            margin-right: 10px; /* Adds space between text, image, and button */
        }

        /* Special Styling for Logout Button */
        .logout-btn {
            background-color: #e74c3c;
            color: white !important;
            padding: 10px 20px;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .logout-btn:hover {
            background-color: #c0392b;
        }

        /* Remove margin from the last button */
        .navbar-right a:last-child {
            margin-right: 0;
        }

        /* Media Query for Smaller Screens */
        @media (max-width: 768px) {
            .navbar {
                flex-direction: column;
                align-items: flex-start;
                padding: 10px;
            }

            .navbar-left a, .navbar-right a {
                margin-right: 0;
                margin-bottom: 10px;
                font-size: 14px;
            }

            .navbar-right {
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar">
        <div class="navbar-left">
            <a href="/" class="logo">Home</a>
            <a href="/blog/get">All Blogs</a>
        </div>
        <div class="navbar-right">
            <% if (!user) { %>
                <a href="/user/signUP">SignUp</a>
                <a href="/user/login">Login</a>
            <% } else { %>
                <span class="welcome-text">Welcome, <%= user.username %> </span>
                <% if (user.image) { %>
                    <img class="user-image" src="/Uploads/<%= user.image %>" alt="User Image" style="height: 30px; width: 30px; border-radius: 50%;">
                <% } %>
                <a href="/user/logout" class="logout-btn">Logout</a>
            <% } %>
        </div>
    </nav>

</body>
</html>
